export default function App() {
  return (
    <main className="min-h-screen bg-background p-8">
      <h1 className="text-2xl font-semibold text-foreground">Vero</h1>
      <p className="mt-2 max-w-prose text-sm text-muted-foreground">
        Simulated AI credit underwriting. Technical demonstration using synthetic data — not a
        real credit decisioning system.
      </p>
    </main>
  )
}
